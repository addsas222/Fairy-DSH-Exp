node_modules/
.dsh-test-home/
.env
.env.*
*.key
*.pem
logs/
sessions/
runtime/
# …但 .agent-presets/fairy/runtime/{index.js,safety-gate.js} 是**显式 -f 跟踪**的例外：
# 它们是公开语料（style/*.json + behavior/*.json）的产物，随仓分发以便任何机器部署后即可跑完整模式。
#
# 两条边界的**风险不在同一侧**：
#   部署/对账侧：无害。 SKIP_POLICY 已把 `.agent-presets/fairy/runtime` 整目录排除
#                （"私有部署资产，不进公开仓库"），放私有真身做 drop-in 不会被判 drift、
#                也不会被部署覆盖——所以**不需要** --preserve，也**不要**在 LOCAL_ADAPTATIONS 里加条目。
#   git 侧：有风险。    它们已是"已跟踪文件"——把私有真身写到同名文件上，一次 `git add -A`
#                就会把私有实现推上公开仓。替换前先脱钩：
#   git update-index --skip-worktree .agent-presets/fairy/runtime/index.js
#   git update-index --skip-worktree .agent-presets/fairy/runtime/safety-gate.js
# （仓库内引擎的替代路径也可以是 plugins/ 下新增文件，而不是覆盖这两个。）
playwright-profile/
world-core/
browser-evidence/
benchmarks/

# ponytail 技能组（MIT，上游再分发内容）只在本机 ~/.omp/agent/skills/_ponytail-vendor/ 留存，
# 不入库、不随部署分发。
.agent-presets/ponytail/skills/

# 绝区零官方文本（借用的第三方游戏资源，版权归 miHoYo）：只在本机供 world-core 使用，
# 不随仓库/部署分发。由 C:/tmp/zzz-extract.mjs 从 dimbreath/ZenlessData TextMap 提取。
.agent-presets/fairy/world-core/
